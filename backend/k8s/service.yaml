apiVersion: v1
kind: Service
metadata:
  name: banking-platform-backend
spec:
  #type: LoadBalancer
  type: ClusterIP
  selector:
    app: backend
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080